
    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );

    create table users (
        id varchar(255) not null,
        login varchar(255) unique,
        password varchar(255),
        role enum ('DRIVER','SUPPLIER'),
        primary key (id)
    );
